rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {

    // Public can read sections and pages
    match /sections/{sectionId} {
      allow read: if true;
      allow write: if request.auth != null && isAdmin();
    }

    match /pages/{pageId} {
      allow read: if true;
      allow write: if request.auth != null && isAdmin();
    }

    // Public can read site settings (active template, phone, site name)
    match /settings/{docId} {
      allow read: if true;
      allow write: if request.auth != null && isSuperAdmin();
    }

    // Admins collection — only superadmin can manage
    match /admins/{docId} {
      allow read: if request.auth != null && isAdmin();
      allow write: if request.auth != null && isSuperAdmin();
    }

    // Service schedule entries — public read, any admin can write
    match /serviceEvents/{dateId} {
      allow read: if true;
      allow write: if request.auth != null && isAdmin();
    }

    // Service templates — public read, any admin can write
    match /serviceTemplates/{templateId} {
      allow read: if true;
      allow write: if request.auth != null && isAdmin();
    }

    // Temporary test document — REMOVE before go-live
    match /_test/{docId} {
      allow read, write: if true;
    }

    function isAdmin() {
      return exists(/databases/$(database)/documents/admins/$(request.auth.uid));
    }

    function isSuperAdmin() {
      return get(/databases/$(database)/documents/admins/$(request.auth.uid)).data.role == 'superadmin';
    }
  }
}
